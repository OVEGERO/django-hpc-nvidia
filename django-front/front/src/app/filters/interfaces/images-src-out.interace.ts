export interface ImagesSrcOut {
  index: string,
  image: string,
  time: string,
  thread_per_block: string,
  number_of_blocks: string,
  method: string,
  kernel: string,
  sigma: string,
}
